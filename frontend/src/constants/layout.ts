/**
 * Centralized layout constants for consistent page and component sizing.
 */

/** Standard page content height accounting for the app header (64px + padding) and footer (~36px). */
export const PAGE_CONTENT_HEIGHT = 'calc(100vh - 156px)'
