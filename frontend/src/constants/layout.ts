/**
 * Centralized layout constants for consistent page and component sizing.
 */

/** Standard page content height accounting for the app header (64px + padding). */
export const PAGE_CONTENT_HEIGHT = 'calc(100vh - 120px)'
