# Frontend Trivy ignore
# ----------------------
# No active suppressions.
#
# History: PAT-157 temporarily ignored axios 1.15.0 HIGH CVEs
# (CVE-2026-42033 / -42035 / -42043 / -42264) because the patched 1.15.1 / 1.15.2
# releases were not yet published to npm. BUG-121 bumped axios to 1.16.1, which
# fixes those four plus the later 1.16.0 set (CVE-2026-44492 / -44494 / -44495),
# so all suppressions were removed.
