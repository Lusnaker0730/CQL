apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: cql-platform
  labels:
    app: prometheus
    app.kubernetes.io/part-of: cql-platform
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
  selector:
    app: prometheus
