apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: cql-platform
  labels:
    app: backend
    app.kubernetes.io/part-of: cql-platform
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: backend
