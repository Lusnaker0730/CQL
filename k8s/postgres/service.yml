apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: cql-platform
  labels:
    app: postgres
    app.kubernetes.io/part-of: cql-platform
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
