apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: cql-platform
  labels:
    app: frontend
    app.kubernetes.io/part-of: cql-platform
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: frontend
